#include <iostream>
#include <string>

int main() {
    int n;
    std::string A, B, C;

    std::cin >> n;
    std::cin >> A >> B;

    for (int i = 0; i < n; ++i) {
        C += A[i];
        C += B[i];
    }

    std::cout << C;
}

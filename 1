#include <iostream>

    int sum_of_num(int first, int second) {
        return first + second;
    }

int main () {
    int first_num;
    int second_num;
    std::cin >> first_num >> second_num;
    std::cout << sum_of_num (first_num, second_num);
}
